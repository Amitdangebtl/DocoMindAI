@model List<MVCSimpleUplode.Models.DocumentViewModel>

<style>
    /* ===== HEADER ===== */
    .admin-page-header {
        background: linear-gradient(135deg, #1e40af, #2563eb, #4f46e5);
        color: #ffffff;
        border-radius: 16px;
        padding: 26px;
        box-shadow: 0 18px 40px rgba(30,64,175,0.35);
    }

    /* ===== TABLE CARD ===== */
    .table-card {
        background: rgba(255,255,255,0.92);
        backdrop-filter: blur(10px);
        border-radius: 16px;
        padding: 20px;
        box-shadow: 0 18px 40px rgba(15,23,42,0.12);
    }

    /* ===== TABLE ===== */
    .table thead {
        background: linear-gradient(90deg, #2563eb, #4f46e5);
        color: #ffffff;
    }

        .table thead th {
            border: none;
            font-weight: 600;
        }

    .table tbody tr:hover {
        background-color: #f1f5ff;
    }

    /* ===== DELETE BUTTON ===== */
    .btn-delete {
        background: linear-gradient(90deg, #ef4444, #dc2626);
        border: none;
        color: #ffffff;
        border-radius: 8px;
        padding: 6px 16px;
        font-weight: 500;
    }

    /* ===== BACK BUTTON ===== */
    .btn-back {
        background: linear-gradient(90deg, #64748b, #475569);
        border: none;
        color: #ffffff;
        border-radius: 10px;
        padding: 10px 24px;
        font-weight: 600;
    }
</style>

<div class="container mt-4">

    <!-- ===== HEADER ===== -->
    <div class="admin-page-header text-center mb-4">
        <h2 class="fw-bold mb-1">📁 All Documents</h2>
        <p class="opacity-75 mb-0">
            Admin view — delete uploaded documents
        </p>
    </div>

    <!-- ===== TABLE ===== -->
    <div class="table-card">
        <table class="table align-middle mb-0">
            <thead>
                <tr>
                    <th>File Name</th>
                    <th>User Id</th>
                    <th style="width:120px;">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var doc in Model)
                {
                    <tr>
                        <td class="fw-medium">@doc.FileName</td>
                        <td class="text-muted">@doc.UserId</td>
                        <td>
                            <form asp-action="Delete"
                                  method="post"
                                  onsubmit="return confirm('Delete this document?');">
                                <input type="hidden"
                                       name="documentId"
                                       value="@doc.DocumentId" />
                                <button type="submit"
                                        class="btn btn-delete btn-sm">
                                    🗑 Delete
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- ===== BACK BUTTON ===== -->
    <div class="mt-4 text-center">
        <a asp-action="Dashboard" class="btn btn-back">
            ⬅ Back to Dashboard
        </a>
    </div>

</div>